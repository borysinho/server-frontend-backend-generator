{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/index.js",
    "dev": "nodemon --exec tsx src/index.ts --ignore",
    "check-ai": "tsx check-ai-config.ts",
    "db:generate": "prisma generate",
    "db:migrate": "prisma db push",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "db:reset": "prisma migrate reset --force",
    "db:init": "npm run db:migrate && npm run db:generate && npm run db:seed",
    "db:backup": "./scripts/db-backup-simple.sh backup",
    "db:restore": "./scripts/db-backup-simple.sh restore",
    "db:list-backups": "./scripts/db-backup-simple.sh list",
    "db:init:prod": "./scripts/init-production.sh",
    "build:prod": "npm run db:migrate:deploy && npm run db:generate && npm run build",
    "vercel-build": "echo '🚀 Iniciando build para Vercel...' && npm run db:migrate:deploy && echo '✅ Migraciones aplicadas' && npm run db:generate && echo '✅ Cliente Prisma generado' && npm run build && echo '✅ Build completado exitosamente'",
    "generate:flutter": "tsx generate-flutter.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.7.2",
    "nodemon": "^3.1.10",
    "socket.io-client": "^4.8.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "@types/archiver": "^6.0.3",
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.19",
    "@types/nodemailer": "^7.0.2",
    "adm-zip": "^0.5.16",
    "archiver": "^7.0.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "nodemailer": "^7.0.9",
    "openai": "^6.8.1",
    "pg": "^8.16.3",
    "prisma": "^6.17.1",
    "socket.io": "^4.8.1"
  }
}
